<div class="container mt-5">
  <div class="row">
    <div class="col-2">
      <div class="card">
        <div class="card-header">ジャンル検索</div>
        <div class="card-body">
          <% @genres.each do |genre| %>
            <p class="card-text"><%= genre.name %></p>
          <% end %>
        </div>
      </div>
    </div>

    <div class="col-8 ml-2 top_img">
      <div class="message mx-auto border border-dark">
        ようこそ、ながのCAKEへ！</br>
        このサイトは、ケーキ販売のECサイトになります。</br>
        会員でない方も商品の閲覧はできますが、</br>
        購入には会員登録が必要です。</br>
      </div>
    </div>
  </div>

  <div class="row">
      <div class="col-8 offset-2">
        <div><h1 class="my-3">新着商品</h1></div>
      </div>
  </div>

  <div class="row">
    <div class="col-8 offset-2 d-flex justify-content-between">
      <% @items.shuffle.first(4).each do |item| %>
        <%= link_to item_path(item.id) do%>
          <div class="card" style="width: 10rem;">
            <%= attachment_image_tag item, :image, format: "jpg", size:'158x158', class: "card-image-top"%>
            <div class="card-body">
              <p class="card-text text-truncate text-dark"><%= item.name %></p>
              <p class="card-text text-dark">¥<%= item.with_tax_price.to_s(:delimited, delimiter: ',') %>(税込)</p>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>

  <div class="row">
    <div class="col-8 offset-2 mt-2 d-flex justify-content-end">
      <%= link_to "全ての商品を見る >", items_path, class: "text-dark"%>
    </div>
  </div>

</div>
