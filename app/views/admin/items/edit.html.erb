<div class="container">
    <h4 class="p-4">
      <span style="background-color: #EEEEEE">商品編集</span>
    </h4>
    <div class="row">
        <div class="col-8 offset-2">
            <%= form_with model:@item, url:admin_item_path, local: true do |f| %>
                <%= render "form", f: f %>
            <% end %>
        </div>
    </div>
</div>
